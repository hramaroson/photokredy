name: photokredy
description: Mobile app providing prepaid phone card fast and easy scanning

version: 0.1.0+1

environment:
  sdk: ">=2.0.0-dev.68.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  flutter_localizations:
    sdk: flutter

  cupertino_icons: ^0.1.2  

  permission_handler: ^2.2.0 
  fluttertoast: ^3.0.3
  preferences: ^1.3.0

  photokredy_core:
    git: 
      url: https://github.com/hramaroson/photokredy_core.git

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true